
<div class="form-text">
<h2>Employee: <%= @employee.name %></a> </h2>
	<h2>Employee ID: <%= @employee.id %></h2>
	
	<% today_parties = @parties.select do |party| %>
		<% party.created_at > Date.yesterday %>
		<% end %>

		<% sum = 0 %>
		<% today_parties.each do |party| %>
		<% sum += party.tips %>
		<% end %>
</div>

	<h2>Tips Made Today: <%= currency(sum) %> </h2>

<div class="link"><a href="/employees/<%=@employee.id%>/edit">Edit <%= @employee.name %></a>
</div>

                                                                             